variables:
  project_name: AngularCiApp
  
jobs: 
  - job: Build
    steps:
    - script: |
        npm install -g @angular/cli
        npm install
        ng build --prod
      displayName: 'npm install and build'
